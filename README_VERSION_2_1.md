# Versión 2.1

Añade filtros para `Fecha Últ. Trámite`:

- Filtrar por año de la fecha del último trámite.
- Filtrar solo expedientes con `Fecha Últ. Trámite` vacía.
- Nueva columna `Estado F. Últ. Trámite`:
  - `Sin iniciar trámite`
  - `Con trámite`

Sustituye en tu repositorio:

- `app.py`
- `database.py`

Después haz commit y push.
