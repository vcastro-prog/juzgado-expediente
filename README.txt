# Corrección

El error:

    name 'quitar_duplicados_espacios' is not defined

se debe a que la función auxiliar no estaba incluida en el parser.

Añade el contenido de `helper_fix.py` al inicio de `parser_pdf.py`
o impórtalo desde allí.

